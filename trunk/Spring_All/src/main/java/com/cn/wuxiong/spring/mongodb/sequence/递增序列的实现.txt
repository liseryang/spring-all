由于MongoDB没有实现递增id的功能，也就是说，这里的id如果需要递增的话，需要自己维护。
怎么维护呢，我的做法如下

每个集合（相当于机构化数据库里面的表）的id的当前值都存在另一个表中，每次需要递增的时候，先将那里对应的id值找出来，加上1就是。

存所有的集合的当前id 的集合的名称叫MongoDBSequence 
其中有3个字段 id; collectionName; currentValue;
分别表示id，集合名词，当前值